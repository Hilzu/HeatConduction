#include <stdio.h>
#include <stdlib.h>
#include "array.h"
#include "utils.h"
#include "math.h"

int main(int argc, char** argv)
{
  if (is_default(argc, argv)) {
    //replace
  }
  if (argc == 1)
  {
    print_help();
    return 0;
  }
  return 0;
}
